# Jimu Studio v1.1.0 acceptance

Status: accepted on 2026-07-19; release publication not claimed

## Identity

| Item | Value |
|---|---|
| Studio version/tag | `1.1.0` / proposed `v1.1.0` |
| Provider contract | `1.2.0` |
| Provider commit | `bf130c33ba3d47bb7239d238b241b853df72f24e` |
| Provider PR / CI | PR #64 / run `29673464926`, passed test and lint |
| OpenAPI fingerprint | `f797f4650bb62753dc09adb9a713b2b45a54c1b70b58365c9466a404d45ac52e` |
| Packaging | source commit; no reproducible binary or container claim |

The accepted source commit is `a4b4b2f17518305b5fc4f4406a17b2ddefaa76a2`, merged through PR #25. No v1.1.0 release URL or publication time is claimed.

## Operation scores

The machine scorecard covers all 24 Provider operations. The minimum is 96, maximum is 98, and mean is 96.75. All five critical controls are true for every operation. `internal/scorecard` rejects operation omissions, contract method/path/role drift, scores below 90, missing evidence, missing evidence files, and false critical controls.

| Group | Operations | Score |
|---|---:|---:|
| Read/contract and server-held migration operations | 12 | 97 |
| Existing identity/workflow/quota mutations | 9 | 96 |
| Metadata create/update, delete preview, and confirmed delete | 3 | 98 |

The authoritative per-operation record is `v1.1.0-operation-scorecard.json`.

## Local evidence completed

| Gate | Result |
|---|---|
| Pinned contract and scorecard | pass |
| `go test ./...` | pass |
| `go test -race ./...` | pass |
| `go vet ./...` | pass |
| `go build ./cmd/studio ./cmd/local-provider` | pass |
| `go test -tags=e2e -timeout=3m ./e2e -count=1` | pass; six Chrome flows |
| `govulncheck ./...` | pass; no vulnerabilities found |
| `git diff --check` | pass |

The first local race command combined a PowerShell-sensitive coverage argument and incorrectly treated `.out` as a package; the same full race suite was immediately rerun without that argument and passed. CI uses the existing Bash coverage command and remains authoritative for the coverage artifact.

## New operation acceptance

- Entity create is explicit, version-zero, duplicate-safe, bounded to 200 dynamic fields, keyboard/label checked, and provider-authoritative.
- Entity deletion requires `studio.metadata.apply` before provider or draft access, a fresh exact-version dependency preview, an opaque same-session server draft, `DELETE_ENTITY`, stable idempotency, and a valid provider receipt.
- A non-deletable dependency plan renders its dependencies without retaining an applicable draft.
- Conflict and replay invalidate the draft and never silently retry deletion.
- The native local Provider runs on loopback only, exercises every role mutation without Docker, and appends a bounded audit record for every mutation.
- Audit, usage, and discrepancies remain intentionally read-only because the contract defines no mutations.

## Protected production evidence

PR CI `29674505175` passed contract 1.2.0 verification, formatting, vet, race/coverage/build, six-flow Chrome E2E, dependency review, and vulnerability scan; PR CodeQL `29674505174` passed. Protected post-merge CI `29674556716` and CodeQL `29674556675` passed at the accepted source commit. Required checks now contain `Contract 1.2.0` and no longer contain obsolete `Contract 1.1.0`. Publication of `v1.1.0` remains a separate explicit decision.

## Rollback

The last released fallback is `2db2c8bcd877174c068f65ed034303c876da7834` (`v1.0.0`). Rollback requires reauthentication because session/draft state is volatile and does not reverse Provider mutations already completed. Provider backups and recovery remain deployment-owner responsibilities.
